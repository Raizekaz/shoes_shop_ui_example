name: authentication_repository
description: Dart package which manages the authentication domain.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  cache:
    path: ../cache
  cloud_firestore: ^4.4.2
  equatable: ^2.0.5
  firebase_auth: ^4.2.1
  firebase_core: ^2.4.0
  firebase_performance_platform_interface: ^0.1.1+27
  flutter:
    sdk: flutter
  google_sign_in: ^5.4.2
  meta: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  plugin_platform_interface: ^2.1.3
  very_good_analysis: ^3.1.0
